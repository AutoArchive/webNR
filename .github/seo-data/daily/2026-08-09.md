# WebNR operations — 2026-08-09

## Scope

This focused cycle kept WebNR's existing local-first browser-reader positioning and added one secondary reader-facing statement: WebNR can serve as an independent browser-based alternative to Legado for web-first reading workflows. It also added a reader-facing explanation article and introduced repeatable real-Chromium user-journey testing as a permanent Quality gate.

This was not a full scheduled source-growth/data-analysis cycle. No new third-party source was integrated in this focused request, and no unavailable analytics export was converted into zero traffic.

## Evidence and product boundary

- Legado's public documentation remains Android-oriented and documents local/network source import.
- The public `gedoor/legado_web_bookshelf` project describes a companion web bookshelf that depends on the phone-side Legado web service and same-LAN access.
- WebNR therefore describes itself only as an **independent browser-based alternative** for overlapping web-first workflows, never as an official Legado web client.
- Arbitrary Legado source JSON remains outside the current drop-in runtime claim. Compatibility stays incremental, clean-room, fixture-backed, and capability-scoped.
- Analytics behavior was unchanged: the reader and canonical website continue to use GA4 `G-DGH8HNQKE4`; reader page views use full browser URLs; Google signals and ad-personalization signals remain disabled.

## Main delivery

- Pull request: #62 `feat: add Legado web-alternative note and browser E2E`
- Final reviewed head: `3d58215260f89bca2bd2f56f072855fbc83878ff`
- Squash merge: `7140fd24277ab357ed8029db8aa8f6c1ecdfe6bd`
- Final Quality workflow: `31303061815`
- Browser job: `93218999672`
- Browser result: 8 tests passed across desktop Chromium and Pixel 7 emulation.

Public/source changes included:

- GitHub README auxiliary Legado statement and compatibility boundary;
- canonical website home-source auxiliary statement in English and Chinese;
- reader empty-library/About auxiliary statement in English and Chinese;
- new article source `docs/blog/posts/2026-08-09-webnr-legado-web-alternative.md`, intended canonical URL `https://www.webnovel.win/blog/2026/08/09/webnr-legado-web-alternative/`;
- permanent Playwright Chromium desktop/mobile user-journey tests.

## Browser findings and repairs

The new Chromium gate found a real PWA registration bug. The Service Worker registration script uses Next.js `afterInteractive`; the old implementation always attached a `window.load` listener. When injection happened after `load`, registration never ran. The fix now registers immediately when `document.readyState === 'complete'` and otherwise waits for one `load` event.

The browser work also found and closed a keyboard-accessibility gap in the library. Novel rows now have button semantics, focusability, Enter/Space activation, visible focus treatment, and selection labels. The E2E suite verifies that an imported TXT remains in IndexedDB across a page reload and can be reopened from the library using the keyboard.

The final browser suite covered, on both desktop Chromium and Pixel 7 emulation:

1. first-use screen and auxiliary Legado positioning;
2. manifest availability and Service Worker registration;
3. local TXT selection, import and rendered reading content;
4. return to library and IndexedDB persistence across reload;
5. keyboard reopening of the stored book;
6. CORS-allowed URL text import;
7. recoverable unsupported-local-file errors;
8. recoverable invalid-URL-scheme errors.

## CI and production-evidence repair

The cycle also found stale repository deployment evidence. `app-pages` and `gh-pages` already identified `a6e9256369b0b2c29f3c80e428708e0b2da4894c`, while `status.md` still recorded the older `ec8ebc389c613fd9d5386ebf20f93a17f6fd51f3`. The verifier also still expected GitHub Pages itself to carry the old noncanonical origin.

The repair refreshed the two stable machine-readable deployment records and changed the documentation verification target to the attributable GitHub Pages build mirror while requiring that mirror to emit the real canonical `www.webnovel.win` URL.

Final PR checks all passed:

- dependency audit;
- ESLint;
- TypeScript;
- production Next.js build;
- source-output validation;
- strict self-verifying MkDocs build;
- canonical/sitemap/RSS/new-article/single-GA4 assertions;
- recorded application/documentation build evidence;
- Chromium desktop/mobile user journeys.

## Post-merge deployment evidence

After squash merge `7140fd24277ab357ed8029db8aa8f6c1ecdfe6bd`:

- `app-pages/build.json` identifies exact commit `7140fd24277ab357ed8029db8aa8f6c1ecdfe6bd`, source `github-actions`;
- `gh-pages/build.json` identifies exact commit `7140fd24277ab357ed8029db8aa8f6c1ecdfe6bd`, source `github-pages-build-mirror`;
- the generated mirror contains `blog/2026/08/09/webnr-legado-web-alternative/index.html`.

The GitHub Pages documentation output remains a build mirror, not the canonical documentation hostname.

## Remaining external blocker

`https://www.webnovel.win/` remains the sole canonical documentation/editorial/search hostname, but its serving route is still controlled by an older external Cloudflare configuration and is not yet attributable to the current reviewed MkDocs build. The repository-side build, canonical metadata, app deployment and documentation mirror are ready; the external Cloudflare route still requires an account with control of the `webnovel.win` zone/project.

Do not promote GitHub Pages to canonical to work around this blocker. Once the Cloudflare route is corrected, acceptance requires `www/build.json` to identify the current rendered site commit, the 2026-08-06, 2026-08-08 and 2026-08-09 reader articles to be served directly on `www`, canonical/sitemap/RSS to remain on `www`, GA4 `G-DGH8HNQKE4` to remain present, and `app.webnovel.win` to remain the reader runtime.

## Next work

- keep Chromium user journeys as a permanent regression gate;
- continue backup/restore, migration, offline and accessibility improvements;
- continue fixture-backed clean-room Legado compatibility;
- resume reader-facing editorial/source schedule while truthfully distinguishing the canonical `www` routing blocker from the working reader and documentation build mirror.
